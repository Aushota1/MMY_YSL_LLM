module counter_down_037 #(parameter W = 8) (
    input  logic clk, rst_n, en,
    output logic [W-1:0] q
);
    always_ff @(posedge clk or negedge rst_n)
        if (!rst_n) q <= 8'd255;
        else if (en) q <= q - 1;
endmodule
