module counter_updown_050 #(parameter W = 16) (
    input  logic clk, rst_n, en, up,
    output logic [W-1:0] q
);
    always_ff @(posedge clk or negedge rst_n)
        if (!rst_n) q <= '0;
        else if (en) q <= up ? q + 1 : q - 1;
endmodule
